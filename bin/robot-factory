#!/bin/bash

echo $CURRENT_GIT_BRANCH
echo $DEV_GIT_BRANCH
echo $CURRENT_GIT_URL
echo $DEV_GIT_URL

if [$CURRENT_GIT_URL == $DEV_GIT_URL] && [$DEV_GIT_BRANCH != $CURRENT_GIT_BRANCH];
then
    cat >frontend/public/robots.txt <<EOL
# Welcome Robots
User-agent: *
Disallow: /
EOL
fi