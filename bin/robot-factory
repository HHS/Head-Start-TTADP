#!/bin/bash

if [$CURRENT_GIT_URL == $SANDBOX_GIT_URL] && [$SANDBOX_GIT_BRANCH != $CURRENT_GIT_BRANCH];
then
    cat >frontend/public/robots.txt <<EOL
# Welcome Robots
User-agent: *
Disallow: /
EOL
fi