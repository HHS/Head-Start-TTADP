get:
  tags:
    - admin
  operationId: getGrantees
  description: Get all grantees
  responses:
    200:
      description: Grantees
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../index.yaml#/components/schemas/grantee'
