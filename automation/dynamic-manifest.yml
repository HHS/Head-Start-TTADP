---
applications:
- name: tta-automation
  stack: cflinuxfs4
  health-check-type: process
  memory: ${MEMORY}
  disk_quota: ${DISK_QUOTA}
  instances: 1
  no-route: true
  buildpacks:
    - ${BUILDPACK}
  command: ${COMMAND}
  services:
    ${BOUND_SERVICES}
