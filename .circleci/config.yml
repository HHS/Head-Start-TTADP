version: 2.1

orbs:
  node: circleci/node@4.0.0

jobs:
  test:
    docker:
      - image: cimg/node:<node-version>
    steps:
      - checkout
      - node/install:
          install-yarn: true
      - run: node --version
      - node/install-packages # Utilize commands in steps