export const root = `http://localhost:8080/api`;

// test('/settings/email', async ({ request }) => {
//   const response = await request.get(`${root}/settings`);
//   const body = await response.body();
//   const json = JSON.parse(String(body));
//   console.log(json);
// });