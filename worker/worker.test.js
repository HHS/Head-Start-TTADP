import processFile from './worker';

describe('File Upload', () => {
  beforeAll(async () => {
  });
  afterAll(async () => {
  });
  afterEach(() => {
    jest.clearAllMocks();
  });

  describe('File Upload Handlers happy path', () => {
    beforeEach(() => {
    });
    it('tests a file upload', async () => {

    });
  });
});
